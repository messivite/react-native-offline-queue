import{_ as s,o as i,c as a,ag as t}from"./chunks/framework.35uRI6hY.js";const k=JSON.parse('{"title":"How It Works","description":"","frontmatter":{},"headers":[],"relativePath":"guide/how-it-works.md","filePath":"guide/how-it-works.md"}'),n={name:"guide/how-it-works.md"};function l(o,e,h,r,d,c){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h1><h2 id="the-big-picture" tabindex="-1">The Big Picture <a class="header-anchor" href="#the-big-picture" aria-label="Permalink to &quot;The Big Picture&quot;">​</a></h2><p>Everything revolves around a single <code>OfflineManager</code> singleton. It&#39;s just a class instance that lives for the entire app lifecycle. It holds the queue in memory, persists it to storage, and runs the sync logic.</p><p>Your components never talk to <code>OfflineManager</code> directly — they use hooks. The hooks read from the manager&#39;s internal stores via <code>useSyncExternalStore</code>, which means React only re-renders the components that actually care about the data that changed.</p><h2 id="what-happens-when-the-user-taps-a-button" tabindex="-1">What happens when the user taps a button <a class="header-anchor" href="#what-happens-when-the-user-taps-a-button" aria-label="Permalink to &quot;What happens when the user taps a button&quot;">​</a></h2><p>Let&#39;s say you have a &quot;Like&quot; button:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">mutateOffline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">isLoading</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">isQueued</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useOfflineMutation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;LIKE_POST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  handler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">payload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> api.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">likePost</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(payload);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onOptimisticSuccess</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setLiked</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h3 id="when-the-user-is-online" tabindex="-1">When the user is online <a class="header-anchor" href="#when-the-user-is-online" aria-label="Permalink to &quot;When the user is online&quot;">​</a></h3><ol><li>User taps → <code>mutateOffline({ postId: 42 })</code> is called</li><li>Hook detects the device is online</li><li><code>handler(payload)</code> runs immediately — your real API call fires</li><li>If it succeeds → <code>onOptimisticSuccess</code> fires, <code>status</code> becomes <code>&#39;success&#39;</code></li><li>If it fails → action is pushed to the queue as a fallback, <code>status</code> becomes <code>&#39;queued&#39;</code></li></ol><h3 id="when-the-user-is-offline" tabindex="-1">When the user is offline <a class="header-anchor" href="#when-the-user-is-offline" aria-label="Permalink to &quot;When the user is offline&quot;">​</a></h3><ol><li>User taps → <code>mutateOffline({ postId: 42 })</code> is called</li><li>Hook detects the device is offline</li><li>Action is pushed to the queue: <code>{ actionName: &#39;LIKE_POST&#39;, payload: { postId: 42 } }</code></li><li>Queue is persisted to storage (MMKV, AsyncStorage, etc.)</li><li><code>onOptimisticSuccess</code> fires → UI updates immediately</li><li><code>status</code> becomes <code>&#39;queued&#39;</code></li></ol><p>No API call happens. The action just waits.</p><h3 id="when-connectivity-returns" tabindex="-1">When connectivity returns <a class="header-anchor" href="#when-connectivity-returns" aria-label="Permalink to &quot;When connectivity returns&quot;">​</a></h3><ol><li><code>OfflineProvider</code> detects network change via NetInfo</li><li>Depending on <code>syncMode</code>: <ul><li><strong>auto</strong> → <code>flushQueue()</code> runs immediately, silently</li><li><strong>manual</strong> → your <code>onOnlineRestore</code> callback fires (you show an Alert, Toast, etc.)</li></ul></li><li><code>flushQueue()</code> loops through every queued action</li><li>For each action, it looks up the handler: per-action handler first, then <code>onSyncAction</code> fallback</li><li>Success → action removed from queue. Failure → <code>retryCount</code> incremented, stays in queue.</li></ol><h2 id="how-handler-resolution-works" tabindex="-1">How handler resolution works <a class="header-anchor" href="#how-handler-resolution-works" aria-label="Permalink to &quot;How handler resolution works&quot;">​</a></h2><p>When the queue flushes, each action needs to find its handler. The resolution order is:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. Check per-action handler registry</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> actionHandlers.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(action.actionName);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. If not found, fall back to global onSyncAction</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">handler </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> onSyncAction) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onSyncAction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(action);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. Neither exists? Action fails with an error.</span></span></code></pre></div><p>Per-action handlers are registered automatically when <code>useOfflineMutation</code> mounts with a <code>handler</code> option. They persist even after the component unmounts — so if the user navigates away from a screen, the handler is still available for sync.</p><h2 id="how-storage-works" tabindex="-1">How storage works <a class="header-anchor" href="#how-storage-works" aria-label="Permalink to &quot;How storage works&quot;">​</a></h2><p>The queue lives in memory (a plain array) for fast access. Every time it changes — push, remove, clear — it&#39;s also persisted to your storage adapter.</p><p>There are two types of adapters:</p><p><strong>Key-value</strong> (MMKV, AsyncStorage, Memory): The entire queue is serialized as one JSON string. Simple, but every write rewrites the whole queue.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// What happens internally</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> storage.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;OFFLINE_QUEUE&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">JSON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stringify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(queue));</span></span></code></pre></div><p><strong>Record-based</strong> (Realm): Each action is a separate database record. Adding or removing one item doesn&#39;t touch the rest. Much better for large queues.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// What happens internally</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> realm.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;OfflineQueueItem&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, action);</span></span></code></pre></div><h2 id="why-usesyncexternalstore" tabindex="-1">Why useSyncExternalStore <a class="header-anchor" href="#why-usesyncexternalstore" aria-label="Permalink to &quot;Why useSyncExternalStore&quot;">​</a></h2><p>Every hook in this package — <code>useNetworkStatus</code>, <code>useOfflineQueue</code>, <code>useSyncProgress</code> — reads from a store on the <code>OfflineManager</code> singleton using React&#39;s <code>useSyncExternalStore</code>.</p><p>This matters because:</p><ul><li><strong>No Context cascading.</strong> When <code>isOnline</code> changes, only components that call <code>useNetworkStatus()</code> re-render. Everything else is untouched.</li><li><strong>No unnecessary renders.</strong> If the queue changes, only <code>useOfflineQueue()</code> consumers re-render. Network status consumers don&#39;t.</li><li><strong>Works outside React.</strong> <code>OfflineManager</code> is a plain class. You can read <code>OfflineManager.isOnline</code> or call <code>OfflineManager.flushQueue()</code> from background tasks, service layers, or anywhere.</li></ul><p>Compare this to a typical Context approach where changing one value re-renders every component inside the Provider.</p><h2 id="mutation-state-lifecycle" tabindex="-1">Mutation state lifecycle <a class="header-anchor" href="#mutation-state-lifecycle" aria-label="Permalink to &quot;Mutation state lifecycle&quot;">​</a></h2><p>Each <code>useOfflineMutation</code> call tracks its own <code>status</code>:</p><table tabindex="0"><thead><tr><th>Scenario</th><th>Flow</th></tr></thead><tbody><tr><td>Online, API succeeds</td><td><code>idle</code> → <code>loading</code> → <code>success</code></td></tr><tr><td>Online, API fails</td><td><code>idle</code> → <code>loading</code> → <code>queued</code> (action saved as fallback)</td></tr><tr><td>Offline</td><td><code>idle</code> → <code>queued</code> (action saved, UI updates optimistically)</td></tr></tbody></table><p>You can use <code>reset()</code> to go back to <code>idle</code> at any time.</p>`,34)])])}const u=s(n,[["render",l]]);export{k as __pageData,u as default};
